# UI-only mode (default): backend, DB y Docker en deshuso. Solo UI + mocks.
VITE_MOCK_API=true

# Para conectar al backend real más adelante:
VITE_MOCK_API=false
VITE_API_URL="http://localhost:9999/api"